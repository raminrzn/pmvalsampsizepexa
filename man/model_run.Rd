% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_run.R
\name{model_run}
\alias{model_run}
\title{Minimum sample size to validate a prediction model (ModelsCloud entry point)}
\usage{
model_run(model_input = NULL, ..., time_limit = 120)
}
\arguments{
\item{model_input}{A named list (or one-row data frame) with \code{type}
(\code{"binary"}), \code{prevalence}, \code{cstatistic}, and one of \code{lpnormal}, \code{lpbeta}
or \code{lpcstat}. Optional: \code{cslope}, \code{csciwidth}, \code{oe}, \code{oeciwidth},
\code{cstatciwidth}, \code{simobs}, \code{seed}, and the net-benefit fields
(\code{sensitivity}, \code{specificity}, \code{threshold}, \code{nbciwidth}). If \code{NULL} and
nothing is supplied via \code{...}, \code{\link[=get_default_input]{get_default_input()}} is used.}

\item{...}{Alternative to \code{model_input}: the fields as named arguments.}

\item{time_limit}{Seconds to allow before aborting, guarding against the
\code{lpcstat} iterative search. Default 120; \code{Inf} disables the guard.}
}
\value{
A named list with the headline \code{sample_size}, the implied number of
\code{events}, the precision achieved for each target (O/E ratio, calibration
slope, C-statistic), and \code{criteria} — a data frame with one row per
sample-size criterion plus the binding "Final SS" row.
}
\description{
Computes the minimum sample size required to externally validate an existing
multivariable clinical prediction model with a \strong{binary} outcome, using the
criteria of Riley et al. (\emph{Stat Med} 2021) and Archer et al. (2020).
}
\details{
Inputs may arrive \strong{wrapped} under \code{model_input} or \strong{unwrapped} as named
arguments, and common aliases are mapped to canonical names (\code{auc} ->
\code{cstatistic}, \code{simulations} -> \code{simobs}).

Three fields are mandatory: \code{prevalence}, \code{cstatistic}, and exactly one
description of the linear predictor distribution — \code{lpnormal} (mean and SD),
\code{lpbeta} (alpha and beta), or \code{lpcstat} (an anticipated C-statistic).

\strong{\code{lpcstat} can be very slow.} When the simulated event proportion does not
match the requested prevalence, pmvalsampsize falls back to an iterative
search that is not guaranteed to terminate quickly; a hosted call would
otherwise hang indefinitely. \code{time_limit} bounds it and fails with an
actionable message. \code{lpnormal} and \code{lpbeta} return in about a second.

Only binary outcomes are supported, because that is all pmvalsampsize 0.1.0
implements — continuous and survival types are rejected rather than passed
through to an obscure internal error.

This is a study-design calculator, not a patient-level risk model.
}
\examples{
model_run(get_default_input())
}
\references{
Riley RD, Debray TPA, Collins GS, et al. Minimum sample size for external
validation of a clinical prediction model with a binary outcome.
\emph{Stat Med.} 2021;40(19):4230-4251. \doi{10.1002/sim.9025}

Archer L, Snell KIE, Ensor J, et al. Minimum sample size for external
validation of a clinical prediction model with a continuous outcome.
\emph{Stat Med.} 2021;40(1):133-146. \doi{10.1002/sim.8766}
}
