% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelscloud_api.R
\name{gateway}
\alias{gateway}
\title{ModelsCloud gateway (server entry point)}
\usage{
gateway(...)
}
\arguments{
\item{...}{Request fields supplied by the platform. \code{func}, \code{api_key},
\code{session_id}, \code{execution_id}, and \code{callback_url} are treated as control
fields; everything else is passed through to the selected function.}
}
\value{
A JSON string produced by \code{\link[jsonlite:fromJSON]{jsonlite::toJSON()}}.
}
\description{
Generic dispatcher invoked by the ModelsCloud platform. Reads \code{func} from the
incoming request, strips the platform-only fields, and calls the named
package function with whatever remains — so
\code{gateway(func = "model_run", model_input = ...)} runs \code{\link[=model_run]{model_run()}}. When
\code{func} is absent it defaults to \code{"model_run"}, which makes the platform's
default entry point land on the model without the caller naming it.
}
